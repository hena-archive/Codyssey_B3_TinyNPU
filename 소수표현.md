# 소수 표현

## 부동소수점 공부 TodoList
 1. 기본 개념
    - 부동소수점이 왜 필요한지
    - 고정소수점 vs 부동소수점
    - float, double의 차이
 2. 표현 방식
    - IEEE 754 이해
    - 부호(Sign) + 지수(Exponent) + 가수(Fraction)
    - 32-bit float / 64-bit double
 3. 핵심 원리
    - 10진수 → 2진수 변환
    - 정규화(Normalization)
    - 지수의 Bias
 4. 오차 이해
    - 0.1 + 0.2 ≠ 0.3인 이유
    - 반올림 오차
    - 표현할 수 없는 숫자
 5. 특수 값
    - +0, -0
    - Infinity
    - NaN
    - Subnormal number
 6. 실전
    - 부동소수점 비교 시 ==의 문제
    - epsilon을 이용한 비교
    - 금융/정밀 계산에서의 대안



## 1. 기본 개념
### 부동소수점이란?
컴퓨터에서 실수를 표현하는 방법이다.
```
123.456
0.00123
-9876.5
```

### 고정소수점 vs 부동소수점
| 방식 |	특징 |
|---|---|
|고정소수점 | 소수점 위치가 고정 |
|부동소수점 |	소수점 위치가 움직일 수 있음 |



### float vs double
일반적인 IEEE 754 기준:
```
float  = 32 bit : 약 7자리 십진 정밀도
double = 64 bit : double → 약 15~16자리 십진 정밀도
```

## 2. 부동소수점의 표현 방식
```
┌─────┬──────────┬───────────────────────┐
│ Sign│ Exponent │       Fraction        │
│ 1bit│  8 bits  │        23 bits        │
└─────┴──────────┴───────────────────────┘

(-1)^Sign × 1.Fraction × 2^(Exponent-Bias)
```

## 3. 2진수와 정규화
컴퓨터는 기본적으로 2진수를 사용한다.

10진수를 2진수로 변경
```
13.25 - 10진수
1101.01₂ - 2진수
```


- 1.10101 × 2³
- 
```
Sign     = 0
Exponent = 3
Fraction = 10101...
```

Bias
지수에는 음수도 필요하다.

그래서 실제 지수 3을 그대로 저장하지 않고 Bias를 더해서 저장한다.

32-bit float에서는:

Bias = 127
```
실제 지수 3
→ 저장되는 지수 3 + 127
→ 130
→ 10000010₂
```
## 4. 부동소수점 오차
컴퓨터가 모든 실수를 정확하게 표현할 수 있는 것은 아니다.

10진수 표현
```
0.1
```


2진수 표현
- 10진수 0.1은 2진수로 표현하면 무한히 반복된다.
```
0.00011001100110011...
```


`0.1 + 0.2` 가 `0.3`이 아닌 이유
```
0.1 + 0.2 = 0.30000000000000004
```

부동소수점은 실수를 정확히 저장하는 것이 아니라, 제한된 비트 안에서 가장 가까운 값을 저장한다.

1. 특수한 값
IEEE 754에는 일반적인 숫자 외에도 특수 값이 있다.

+0 / -0
+0
-0




둘 다 0처럼 보이지만 비트 표현은 다르다.

Infinity
범위를 초과하면:

+∞
-∞




가 될 수 있다.

예:

1.0 / 0.0 → +Infinity




NaN
Not a Number

숫자로 정의할 수 없는 연산의 결과다.

예:

0.0 / 0.0 → NaN




주의할 점:

NaN == NaN




은 일반적으로 false다.

## 6. 부동소수점 비교
다음 코드는 위험할 수 있다.
```
a == b
```



부동소수점에서는 아주 작은 오차가 존재할 수 있기 때문이다.

대신 **허용 오차(epsilon)**를 사용한다.

개념적으로:

|a - b| < ε




이면 같은 값으로 취급한다.

예를 들어:

a = 0.3000000001
b = 0.3




이라면 아주 작은 차이는 무시할 수 있다.

단, epsilon을 무조건 고정된 하나의 값으로 쓰는 것이 항상 올바른 것은 아니다. 값의 크기에 따라 상대 오차까지 고려해야 하는 경우가 있다.

7. 실전에서의 주의점
돈 계산
금융 계산에서 float나 double을 그대로 사용하는 것은 주의해야 한다.

0.1 + 0.2




같은 오차가 금액 계산에 누적될 수 있기 때문이다.

상황에 따라:

정수로 최소 단위(예: 원) 저장
Decimal/BigDecimal 계열 사용
등을 고려한다.

오차가 누적될 수 있다
반복적인 계산에서는 작은 오차가 계속 쌓일 수 있다.

계산 → 반올림 오차 → 계산 → 반올림 오차 → ...




따라서 정밀도가 중요한 계산인지 먼저 판단해야 한다.

🧠 최종 압축
부동소수점
│
├─ 왜?
│   └─ 큰 수 ~ 작은 수를 효율적으로 표현
│
├─ 어떻게?
│   └─ Sign + Exponent + Fraction
│
├─ 기준
│   └─ IEEE 754
│
├─ float
│   └─ 32bit / 약 7자리 정밀도
│
├─ double
│   └─ 64bit / 약 15~16자리 정밀도
│
├─ 핵심 문제
│   └─ 모든 10진수를 2진수로 정확히 표현할 수 없음
│
├─ 결과
│   └─ 반올림 오차 발생
│
├─ 특수 값
│   ├─ ±0
│   ├─ ±Infinity
│   └─ NaN
│
└─ 실전
    ├─ float/double 직접 비교 주의
    ├─ epsilon 활용
    └─ 금융/정밀 계산은 Decimal 계열 고려